syntax = "proto3";

package photograftai;

service MaskService {
  rpc GetMask(GetMaskRequest) returns (GetMaskResponse) {}
}

message GetMaskRequest {
  string image = 1;
  bytes points = 2;
}

message GetMaskResponse {
  repeated string masks = 1;
}